export enum Network {
  MAINNET = 'mainnet',
  TESTNET = 'testnet',
}

export enum KeyType {
  ED25519 = 'ed25519',
  ECDSA = 'ecdsa',
}

export enum DealAlias {
  RECEIVER = 'receiver',
  PAYER = 'payer',
}
